apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: trip-manager-prod
  namespace: argocd
spec:
  generators:
    - git:
        repoURL: https://github.com/trip-manager-htwg/gitops.git
        revision: main
        directories:
          - path: prod/*
          - path: prod/external-secrets
            exclude: true
          - path: prod/cluster-config
            exclude: true
  template:
    metadata:
      name: '{{path.basename}}'
      namespace: argocd
    spec:
      project: default
      source:
        repoURL: https://github.com/trip-manager-htwg/gitops.git
        targetRevision: main
        path: '{{path}}'
        directory:
          recurse: true
      destination:
        server: https://kubernetes.default.svc
        namespace: trip-manager-prod
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true